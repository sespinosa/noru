import AIPanel from "../components/AIPanel";

export default function TranscriptViewer() {
  return (
    <div>
      <p className="placeholder">Transcript viewer placeholder. (Phase 2 frontend teammate)</p>
      <AIPanel />
    </div>
  );
}
